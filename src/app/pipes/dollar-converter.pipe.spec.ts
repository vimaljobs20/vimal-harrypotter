import { DollarConverterPipe } from './dollar-converter.pipe';

describe('DollarConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
